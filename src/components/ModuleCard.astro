---
import { Icon } from 'astro-icon/components';
interface Props {
  iconName: string;
  title: string;
  body: string;
  revealDelay?: number;
}
const { iconName, title, body, revealDelay = 0 } = Astro.props;
---
<article class="reveal-on-scroll bg-white border border-slate-200 rounded-lg p-4 hover:border-primary-500 hover:shadow-sm transition-all" style={`--reveal-delay: ${revealDelay}ms`}>
  <Icon name={iconName} size={20} class="text-primary-700 mb-3" />
  <h3 class="text-sm font-semibold text-primary-900">{title}</h3>
  <p class="text-xs text-ink-600 leading-relaxed mt-1">{body}</p>
</article>

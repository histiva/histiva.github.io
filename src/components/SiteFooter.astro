---
import { type Locale, t, localizedPath } from '~/i18n/utils';
import LanguageSwitcher from './LanguageSwitcher.astro';
interface Props { locale: Locale; pathname: string; }
const { locale, pathname } = Astro.props;
const year = new Date().getFullYear();
---
<footer class="mt-16 bg-primary-900 text-primary-100">
  <div class="container-page py-12 grid gap-8 md:grid-cols-4">
    <div>
      <p class="text-xl font-bold text-white">MediSina</p>
      <p class="text-sm mt-2 text-primary-100/90">{t('footer.tagline', locale)}</p>
    </div>
    <div>
      <p class="text-xs uppercase tracking-wider text-primary-100/80 mb-3">{t('footer.about', locale)}</p>
      <ul class="space-y-2 text-sm">
        <li><a href={localizedPath('/about', locale)} class="text-white hover:underline">{t('nav.about', locale)}</a></li>
        <li><a href={localizedPath('/ai', locale)} class="text-white hover:underline">{t('nav.ai', locale)}</a></li>
        <li><a href={localizedPath('/product', locale)} class="text-white hover:underline">{t('nav.product', locale)}</a></li>
      </ul>
    </div>
    <div>
      <p class="text-xs uppercase tracking-wider text-primary-100/80 mb-3">{t('footer.connect', locale)}</p>
      <ul class="space-y-2 text-sm">
        <li><a href={localizedPath('/contact', locale)} class="text-white hover:underline">{t('nav.contact', locale)}</a></li>
      </ul>
    </div>
    <div>
      <p class="text-xs uppercase tracking-wider text-primary-100/80 mb-3">{t('footer.legal', locale)}</p>
      <ul class="space-y-2 text-sm">
        <li><a href={localizedPath('/privacy', locale)} class="text-white hover:underline">{t('footer.privacy', locale)}</a></li>
      </ul>
      <div class="mt-4"><LanguageSwitcher current={locale} pathname={pathname} onDark /></div>
    </div>
  </div>
  <div class="border-t border-primary-700/40">
    <div class="container-page py-4 text-xs text-primary-100/90 flex flex-wrap items-center justify-between gap-2">
      <span>© {year} MediSina · {t('footer.builtwith', locale)}</span>
      <span>{t('footer.proprietary', locale)}</span>
    </div>
  </div>
</footer>
